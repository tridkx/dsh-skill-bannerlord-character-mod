---
name: bannerlord-character-mod
description: 把别的游戏/来源的角色模型做成《骑马与砍杀2：霸主》的装备（皮套）mod：提取原始蒙皮权重、几百根骨→27 骨骼映射、姿态重定向、.tpac 资产打包（网格/贴图/材质）、装备 XML、离线与实机验证，以及移植后各类显示问题的定位（颜色错位、纯黑/不透明白片、脸型扭曲、手脚错位、站姿怪异、脖子被拉长、整块模型变刚体、裙摆折痕、膝关节塌角、原版身体露出来）。
whenToUse: 用户要把某个角色模型（FBX/OBJ/.blend/游戏提取件）做成骑砍2 的装备/皮套/换装 mod；或要排查已移植模型在游戏里的显示问题（颜色错位、镜片/头发/睫毛不透明、脸型扭曲、拇指或手肘异常、站姿怪、原版脚露出来）。也可用于给骑砍2 手写 .tpac 资产包。
user-invocable: true
---

# 骑砍2 角色皮套 mod 制作 Skill

把外部角色模型移植成 Bannerlord 装备件。**整条链路已跑通过三次完整工程**（RE:Resistance / RE4R / 仙剑七），
覆盖两个不同的源引擎（RE Engine、UE4+glTF）、两种权重槽数与两种 UV 约定，可直接复用：

| 资源 | 位置 |
|---|---|
| `.tpac` 工具集（打包器 + Python 工具 + 格式文档） | `D:\mb-tools`（公开：https://github.com/tridkx/bannerlord-tpac-toolkit ） |
| 现成对照 mod（官方 Modding Kit 产出、显示正常） | `<游戏>\Modules\LVBU and DIAOCHAN\` |
| 游戏 | `D:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord` |

**两个已跑通的完整工程**（可直接抄结构与脚本，都带完整技术记录）：

| 工程 | 源 | 特点 | 仓库 |
|---|---|---|---|
| RE4R 艾达·王 旗袍 | RE Engine `.mesh`（**8 槽权重**、405 骨、bottom-origin UV） | 单体角色，含高跟鞋/旗袍 | `D:\dsh-mod\mb2mod-ada`（https://github.com/tridkx/bannerlord-ada-wong-costumes ） |
| 仙剑七 月清疏/白茉晴 | UE4.25 导出的 **glTF/GLB**（**4 槽权重**、191/264 骨、**top-origin UV**、两人共 6 件装备） | ★ 多角色、glTF 管线、离线摆姿势工装、权重后处理 | `D:\dsh-mod\mb-xianjian7`（https://github.com/tridkx/bannerlord-xianjian7-outfits ） |

> 两个工程的源引擎、坐标系、UV 约定、权重槽数**全都不同** —— 这正是下面"换模型必须重新量"
> 那几条反复强调的原因。

> **先看成品再动手**：拿参考 mod 的 `pack0.tpac` 用 `mbtool matall` / `mbtool mesh` / `mbtool mat`
> dump 一遍，就知道"正常长什么样"。移植里绝大多数参数问题都是靠这个差集定位的。

---

## 0. 交付物形态（先建立正确预期）

一个装备 mod = **三个文件**：

```
Modules\<ModName>\
    SubModule.xml                 模块声明（Id/Name/依赖/挂 items.xml）
    ModuleData\items.xml          物品定义（mesh 名 → 物品，槽位，护甲值）
    AssetPackages\pack0.tpac      资产包（网格 + 贴图 + 材质，全部在这里）
```

不需要 C# DLL、不需要 Modding Kit 工程 —— 只要你有一个能写 `.tpac` 的工具（见 §4）。

---

## 1. 先体检源模型（决定难度与路线）

**这一步决定后面所有工作量**，别跳。检查项：

| 检查 | 为什么关键 |
|---|---|
| 有没有骨骼？ | 没骨骼 = 要自己摆姿势、对齐骨架 |
| **有没有蒙皮权重？权重在哪个文件里？** | 最关键的变量，见 §2 |
| 骨架骨名/骨数 | 决定映射表怎么写 |
| 材质分组与贴图通道 | 决定材质与贴图怎么拆 |
| 面数 / 部件数 / LOD | 决定性能与是否拆件 |

常用脚本：体检用的 intake report 脚本（面数/材质/UV/缝合片/骨架一览）。

---

## 2. ★ 权重：优先用源模型的**原始权重**，不要自己算

**本 skill 最重要的一条经验。**

- OBJ **不携带**蒙皮权重。如果源模型是"从游戏里导出的 OBJ"，很容易以为没有权重 ——
  但**原始 `.mesh`（或 FBX/glTF）里通常仍在**，只是导出格式丢了。
- 实测：RE Engine 的 `.mesh` 里 100% 顶点带权、每顶点 8 槽、精确归一（每顶点权重和 = 1.0000）。
- 用 `tools/export_weights.py` 解析原始网格并**与 OBJ 顶点逐组对齐**（实测最大偏差 5e-07 = float32 打印精度）。
- 自己按"到骨段距离"算权重，在**面部微骨（几百根）与手指**这些地方必然出错 —— 上一版
  的"脸型扭曲 + 大拇指弯折"就是自算权重的产物。

权重索引的坐标系要确认：RE 的权重索引指向 `skeleton.json` 的 `weightedBones` 表（**不是全骨表**），
用"索引范围 < len(weightedBones)"校验即可。

---

## 3. 骨骼映射：源骨架几百根 → 27 根，规则要能审计

> 已实测过的规模：RE:Resistance 405 根 / RE4R 375 根（**8 槽**权重）、
> UE4 Biped 191 与 264 根（**4 槽**权重）。流程一样，差别在细节规则（见下面的 UE4 三条）。

Bannerlord 人形骨架 = 28 个引擎索引（**名字末尾的 `_N` 才是引擎索引**，FBX 导入后的列表下标不同，
中间夹着 `_notused` 骨）。映射规则按优先级：

| 规则 | 说明 |
|---|---|
| 主骨显式表 | 24 根主骨 → 引擎索引，手工核定 |
| ★ 面部/头发 → **head(13)** | 靠"骨链位置"会把它们判到 **neck(12)** —— 脸大部分在 head 关节(1.573m) **之下**、离 neck(1.466m) 更近。Bannerlord 的头是单根刚体骨，面部微骨必须显式归 head |
| ★ 手指 → **hand(19/26)** | 散到前臂扭曲骨(18/25) 会让同一只手被两根骨撕开 = 实测的"大拇指弯折" |
| 扭曲骨按段 | `*_humerus_twist*`→16/23、`*_radius_twist*`→18/25；腿无扭曲骨 → 归股/胫 |
| 背包/背带/布料 → 躯干链 | 否则背包跟着手臂甩 |
| 其余 → 骨链分数匹配 | 算该骨在最近骨链上的归一化位置 s，取目标骨链上分数最接近的骨。**分数与姿势无关**，因此不受"源绑定姿势 vs BL A-pose"影响 |
| ★★ 辅助骨/扭曲骨：**名字只能当假设，必须用位置复核** | 名字会骗人。RE4 的 `L_Toe_Twist_s` 名字带 `Toe`、实际位置在 **z=0.350（小腿中段）**（它是挂在小腿骨上的辅助骨）；按名字归到脚趾骨(4/8) 后，**小腿一整片权重被脚趾骨拖走** —— 实机表现是"小腿不自然弯曲"，脚踝一动小腿就扭。`L_Shin_s` 反过来：名字是 Shin、位置却在脚踝(0.159) |

**★ 位置校验否决（veto）**：名字匹配之后，再算一次「该骨位置 → 名义骨段的距离」，
若它明显比最近的骨段差（实测用 `>4cm 且 >1.8 倍`），就以**位置**为准。
例外：BL 的扭曲骨 16/18/23/25 自己不在骨链表里（没有骨段），无从比较 —— 这类**跳过否决**，
否则会被就近段抢走（实测会把前臂扭曲骨误判到上臂）。

**★ 权重空间审计（`tools/audit_bones.py` 模式）**：对每根引擎索引统计
「拿到权重的顶点，离这根骨自己的骨段有多远」，超阈值的标出来。这是抓"权重跑到别的骨上"的
通用手段 —— 上面那个小腿 bug 就是它抓出来的（脚趾骨的权重顶点 z 范围高达 [0.002, **0.511**]，
中位距离 15.2cm；修好后只剩 [0.002, 0.061]）。
**注意三处"距离大但正常"**：骨盆(0) 收了整条裙摆布料链；手(19/26) 因为源绑定姿势手臂在身前、
与 BL 的 A-pose 手位天然差得远；躯干骨的距离≈身体半径。

**UE4 Biped（`Bip001-*` / glTF 源）的额外三条**：

- **面部微骨必须显式列名**：`Bizi`(鼻) `Zuijiao`(嘴角) `SZuichun/XZuichun`(上下唇) `Xiaba`(下巴)
  `Tongue_*`(舌) `Yanmei*`(眼眉) `YanJiao*`(眼角) `Yanpi`(眼皮) `Lian*`(脸) `Meixin`(眉心)
  `Quangu`(颧骨) `Eyejoint*` —— 实测一次就有 48~52 根，靠骨链匹配会大量掉到 neck。
- **★★ 头发链必须"强制归 head"且跳过位置否决**：`Bip001-Ponytail*` / `liuhai_*`(刘海) 在源里
  从颅顶一路挂到**腰**（实测 `Ponytail14` 在 z=1.103）。按骨链最近会判给 `spine/neck` ——
  那样一转头头发就留在原地。做法：这类骨走 `FORCE` 规则，**不做位置复核**。
- `root` / `Bip001` / `Skate_Root` / `Weapon_Body` 这类挂点骨一律 → pelvis(0)（多数权重为 0，不影响）。

**交叉验证技巧**：身体网格的 head 权重应当是 0%（身体不含头），脸≈99%、头发=100% ——
三件互相印证，能立刻发现映射错位。权重的 8→4 槽归并几乎无损（实测脸 0.0001%，因为微骨归并到同一根骨）。

---

## 4. 姿态重定向（把源绑定姿势搬进 BL 的 A-pose）

逐骨变换（引擎空间：Z 上、+Y 前、+X 右）：

```
W_b(v) = u_b + s · Rot_b · (v − t_b)
  s, R    全局相似变换（Umeyama，只用躯干+腿拟合，**别把前伸的手臂算进去**）
  t_b/u_b 源骨/BL 骨的 head
  Rot_b = M_b · R，M_b = 把源骨轴（经 R 旋转）转到 BL 骨轴的最小旋转
```

要点：

1. **骨轴必须用"关节位置差"推导**，不能从骨骼矩阵取（源工程有的骨轴在矩阵 X 列、有的在 Y 列；
   Blender 导入的 BL 骨 Y 轴与子骨方向差 90°）。用一张手工核定的"骨链下一关节"表。
2. **判断手臂方案要量化，别靠感觉**：量"腕部落点 vs BL 手关节"的偏差。
   - 逐骨最小旋转 + **链式落点**（`u_w = u_r + s·Rot_r·(t_w − t_r)`）= 偏差 3.9cm（方向正确）
   - 整条手臂刚性 = 偏差 21.8cm 且**朝前 16cm**（双手一直端着）→ 否掉
   - 那 3.9cm 的来源是长度差（源前臂×s 22.9cm vs BL 26.8cm），不是错误
3. 手腕"共用前臂旋转 + 落点沿链推出"能同时消掉扇叶与接缝。
4. 姿态微调旋钮（都绕关节转，可归零）：
   - `SHOULDER_FWD_DEG`：绕**锁骨根部**只转肩+手臂（躯干不动）→ 治"肩臂太靠后"
   - 不要用"整体前倾躯干"来治肩臂靠后 —— 那会把肚子/胸一起推出去（实测被用户否掉）
   - `HEAD_SCALE`：头偏小。**必须带渐隐**（见 4.7）
   - 横向体型缩放治"太壮/太小"。**身体和衣服要用同一个系数**（按**骨权重**算系数就天然一致，
     不要按材质分），否则身体撑破衣服。三条实测边界：
     - **躯干与腿必须分开调**：腿和靴子共用小腿/脚骨，腿一放大**靴子也被撑成钝头**。
       实测同一轮反馈先要"再壮一点"（躯干 1.18）、紧接着就是"腿太粗了"（腿回 1.00）。
     - **手臂不能整体放大**，否则手偏离 hand 骨、持械错位。做法：手臂放大到**腕部渐隐回 1.0**。
     - ★ **缩放中心取"该顶点所属骨轴的 (x,y)"，不是全局中轴 x=0**：绕全局中轴放大 12%
       会把腿整体往外推 1.2cm（腿骨还在原位）⇒ 一动就外八字。绕各自骨轴放大只让肢体变粗。
     - 参考值：纵向拟合缩放 1.12 ⇒ 横向补到 1.12 才是"还原源比例"；月清疏躯干 1.18 / 腿 1.00。
5. **剔除隐藏"缝合片"：阈值必须按模型重新量，不能照搬。**
   上一版的经验是"静止姿势最长边 >6cm 就删"，换一个模型后实测**删掉了旗袍 4.68% 的面积**
   （256 面、单面均值 715mm²、最长边中位 11.4cm）—— 那是裙摆正常的大三角面，删了就是破洞。
   真正该删的是**窄长三角**（细长比 `slim = L²/4A` 大），占面积仅 0.02%~0.38%。
   判据要在**缩放/比例修正之前**算 —— 否则把缩放后的正常三角一并误删。
6. **★ 骨架"约定差异"要单独处理，不能一律把关节钉到 BL 位置。**
   源骨架和 BL 骨架对同一个关节的**摆放高度/位置**常常不同，硬钉过去会把网格撕开：
   - **骨盆**：RE4 的 `Hip` 与 `Spine_0` **完全同位**（都在 z=1.015），而 BL pelvis(0) 在 0.915、
     spine(9) 在 1.009。照搬 BL 位置会让髋部网格比脊柱网格低 9.4cm —— 两片在源里本是重合的，
     腰部就被撕开。做法：给骨盆一个**锚点覆盖** `u_0 = BL_POS[9]`（保住源内部几何，
     同时仍绑在 pelvis 骨上、动画行为正确）。
   - **脚踝/地面**：源穿高跟鞋，脚踝离鞋底 14.7cm；BL 骨架脚踝只离地 10.7cm
     （拿参考 mod 的鞋底 z=−0.014 当基准）。钉过去鞋底就掉到 −0.077（**陷进地面 6.3cm**）。
     做法：给下半身一个**竖直抬升场** —— 脚踝以下抬满、往上渐隐到髋部归零，
     这样鞋底回到地面而髋/脊柱/头/手臂仍精确落在 BL 关节上（身高不增加）。
     ⚠️ 它等价于**把小腿压短**了抬升量那么多，人会变矮（实测矮 4.9cm）—— 见 §4.9。
   - ★ **脚踝→脚趾的向量方向**：源（高跟鞋）是 14.15cm **几乎朝下**，BL 是 15.05cm **主要朝前**。
     两边各自钉到 BL 关节位置的话，脚掌上任何"脚骨+趾骨混合权重"的顶点会被两个相差
     **7.63cm** 的落点对拉 —— 实机反馈"**前脚掌变扁了**"。
     做法：**趾骨落点由脚骨变换链式推出**（与手腕那条同一手法）。
     自查：同一个点用两根骨的变换各算一次落点，差值应 ~0（修好前处处 7.63cm）。
   - ★ **脚骨"朝向"约定**：源穿高跟鞋时脚骨轴向下 **66.6°**，BL 平脚只向下 **39.1°**，差 **27.8°**。
     逐骨最小旋转会把这个差**补到脚骨上 = 把整只鞋连竖直的细高跟一起转过去** ——
     实机反馈"高跟鞋鞋跟变斜了，不是竖直的"。
     **做法：脚/趾不做轴向对齐**（`FOOT_NO_ALIGN`），只用全局相似变换 R。
     判据：**鞋子/手套这类"连姿态一起建模"的部件，保持源姿态比对齐骨轴更重要**。
   **通用判据**：量「源关节到某个固定参考（地面/另一关节）的距离」vs「BL 的同一个距离」，
   差值就是这个约定差异的大小；再量一遍**骨轴方向**的夹角，看这个差会不会被"最小旋转"补进去。
7. **头部放大要带渐隐，否则会撕开领口。** 脸/头发网格往往往下延伸到脖子（实测到 z=1.359），
   而衣领是**另一件装备**。整体绕颈关节放大会把脖子下缘拉下去、和领口撕开。
   做法：z≤(颈关节−3cm) 不动、z≥(颈关节+3cm) 全量，中间 smoothstep 过渡。
9. **"看着太矮/太高"的取舍**：整体均匀放大（`GLOBAL_SCALE`）要**绕地面 z=0** 缩放，
   否则脚底会离地或陷地。但它**不同时缩放 BL 的关节位置**，于是关节相对骨架有
   `(k−1)×高度` 的偏移（k=1.07 时踝 +0.7cm、膝 +3.5cm、髋 +6.4cm、**肩 +9.9cm**）——
   肩部那 10cm 意味着**大幅挥砍时手臂旋转支点偏低**，站立/行走看不出来。
   **想要更高的正确做法是调游戏里角色的身高（体型属性）** —— 骨架与装备一起放大、零偏移；
   属于"看起来对"与"关节严格对齐"之间的取舍，做成一行常量、并把这个代价讲清楚。
   （与之相对：抬升场让人变矮、整体缩放让人变高但关节偏移，两个都要能调。）
8. **长裙/长袍必须"跟随腿部"，否则必然穿模。** 布料链（`Skirt_CS_*` 之类）若整条归到骨盆，
   裙子就是**钢体** —— 腿一动就穿出裙面（实机反馈"旗袍和腿穿模"）。源游戏靠布料模拟解决，
   Bannerlord 没有布料。做法：按位置把**一部分**骨盆权重让给腿骨 ——
   裙腰仍跟骨盆、越往下让得越多（实测让出比例 0→0.8），高于膝给大腿、低于膝给小腿。

   ★★ **但绝对不要按 `|x|` 分左右（"这半边跟左腿、那半边跟右腿"）。**
   裙子是**一整圈**的：按侧别分配会在**中缝**形成硬边界，迈步时左半边向前、右半边向后
   ⇒ 裙子被**剪切** ⇒ 一道竖折痕（实机反馈"裙摆不正常弯折"，为此查了两轮）。
   **正确做法：左右对称** —— 同一高度上左腿右腿各拿一半：

   ```python
   w_leg   = SKIRT_LEG_MAX * smoothstep((SKIRT_TOP_Z - z)/(SKIRT_TOP_Z - SKIRT_BOT_Z))
   w_thigh_L = w_thigh_R = w_leg/2      # 高于膝
   w_calf_L  = w_calf_R  = w_leg/2      # 低于膝
   ```
   裙子只随"两腿的平均运动"平移/旋转，横向上**没有任何边界**。代价：单腿大幅前摆时
   裙子不会被单侧掀起（换来的是不折、不穿模）。

   ★ **门的阈值必须按本模型的裙长定，不能用绝对 z。** 实测同一套参数下
   白茉晴的长裙（下摆 z≈0.45）改完明显变好、月清疏的短裙（下摆 z≈0.72）几乎没变 ——
   因为"低于 0.62 不算裙子"那条门是为排脚踝设的，把短裙整个削掉了。
   **先打 cloth 组件的 z 直方图，再定 TOP/BOT。**

   ★★ **给布料做"位置重算权重"时必须排除手脚末端。** 判据"离腿骨链 > 9cm 就是悬空的布"
   会把**脚趾尖**选中 —— BL 的脚趾骨很短，脚趾尖离腿骨链有 **19cm**；被改成骨盆/大腿后，
   鞋底被外推到 **−0.088（陷地 7cm）**。要加两条硬门：**脚踝以下不算裙子**、
   **手脚末端权重(3/4/7/8/19/20/26/27) > 0.3 的一律排除**。

10. ★★ **归并到 27 骨之后，一定要做"权重后处理"，否则关节在动画里是硬折角。**
    源骨架几百根骨（含扭曲骨/布料骨）→ BL 27 骨，**过渡带会缩到只剩一两圈边**。
    实测膝部（关节 12cm 邻域）**单骨顶点占 22.5%** —— 屈膝时膝部塌成多面体
    （实机反馈"**膝关节错位**"）。两个 pass，都在 `remap` 之后、取 top4 之前做：

    | pass | 做法 | 效果 |
    |---|---|---|
    | **沿网格边权重平滑**（Laplacian） | 用面片建边，迭代 `w ← (1−λ)w + λ·mean(邻居)`，3 轮 × λ=0.5 | 膝部单骨占比 22.5% → **3.9%** |
    | **沿股骨的宽单调混合** | 对 (大腿↔小腿)、(上臂↔前臂) 只重分配这两根骨的权重池，按沿股骨的归一化位置铺成一条斜坡 | 折角变圆；`LIMB_BAND=0.16`（±8cm） |

    ★ **带宽必须大于源本身的过渡带**：先用 9cm 试过同一招却**更差** ——
      带外顶点被一刀切成单骨，反而更硬。判据：做完量"关节邻域单骨占比"，必须下降。
    ★★ **手脚末端必须排除在平滑之外**：平滑会把小腿/前臂权重渗到鞋底/手心，而重定向是
      **逐骨刚性映射**，骨段之外的顶点会被**外推** —— 实测鞋底粘上 99.9% 小腿权重后
      从 −0.014 掉到 **−0.088（陷地 7cm）**。
    ⚠️ **但也要知道这条路的天花板**：如果关节邻域顶点太少（实测膝部 12cm 内只有 102 个顶点），
      权重再怎么平滑也变不出多边形 —— 那时该考虑**局部细分网格**，不要再加平滑轮数。

---

## 5. `.tpac` 打包（模板克隆）

所有资产都从**已有包里的模板资产克隆**（模板提供 shader、顶点布局等难构造字段），只换数据/名字/GUID：

```bash
mbtool build spec.json     # spec 里 meshes / materials / textures / templates
```

要点：

- **`.mgeo` 中间格式**（见 `D:\mb-tools\README.md`）：`n×4 u8 骨骼索引 | n×4 u8 骨骼权重`，
  索引 = BL 引擎索引 0..27，**权重是 0..255 的 u8，不是 0..1 的 float**
- ★★ **权重必须先量化成 u8 0..255 再写 `.mgeo`。**
  上游 npz 里存的是 0..1 的 float，直接 `np.asarray(w, np.uint8)` 是**截断** ——
  只有恰好 1.0 的权重变成 1，其余全变 0，于是绝大多数顶点拿到**全零权重向量**，
  引擎只能退化成一整块跟着骨盆走 ⇒ 实机反馈"**她俩变成刚体了，整个模型一起摇晃而不是各个部位分开**"。
  自查（**从最终 `.tpac` 读回来**，不是查中间产物）：

  ```python
  # 解出 VertexStreamData 段后按尺寸表定位第 9/10 个数组（BoneWeights / BoneIndices）
  ss = bw.sum(1)                      # 每顶点 4 个 u8 的和
  assert (np.abs(ss - 255) <= 1).mean() > 0.999     # 应为 100%
  assert bix.max() <= 27
  ```
  修复前实测 `每行和 均值 0.25 / 最大 1 / ==255 占比 0.0%`，修复后 100%。
- ★★ **`.mgeo` 的文件名必须每个材质组唯一。**
  一个 metamesh 里**同一个材质往往有多个组**（实测 `xj7_yue_body` 的 12 个组只用了 4 个材质）。
  用 `<item>__<材质名>.mgeo` 会**同名互相覆盖**，最后只有"该材质的最后一个组"活下来。
  症状极具迷惑性、而且**自相矛盾**：实机里"同一材质的**袖子在、上衣不在**""**靴子在、裙子不在**"，
  而离线渲染（直接读 npz）完全正常。判据：`mbtool mesh <pack> <mesh>` 打出来的子网格里
  只有几种几何在重复，且总面数远小于规格。**做法：文件名带上组序号**，
  并在验证里断言"每个材质组都能对上打包后的子网格（用子集和精确匹配，因为大组会被拆）"。
- **★ UV 的 V 轴方向必须实测，不能照搬经验。** 引擎的 v=0 在贴图**顶行**，而 `.bc` 按 PNG 行序写盘。
  本源到底是 D3D 习惯（v=0 在顶）还是 bottom-origin，**要量**：
  取一个有明确上下的区域（脸最方便），打印各高度的 v 中位数 ——
  实测 RE4 的脸是 颅顶 v≈0.96 / 鼻尖 0.42 / 颈 0.03，**v 随高度递减 = bottom-origin**，
  与上一版工程相反。翻错了的表现是"五官整体上下错位"（鼻子跑到额头上、嘴变黑条）。
  **渲染验收脚本与 `.tpac` 的翻法可能相反**（Blender 的 v=0 也在图片底部），要分别确认。
  `.tpac` 侧必须在**算切线之前**翻，否则切线手性跟着错（颜色错位 + 法线假褶皱）
- **`.bc` 必须带完整 mip 链**，否则打包器报 `blob too small for mip 1`
- 顶点/索引 ≥60000 时打包器自动拆子网格
- 材质模板从原版 `materials.tpac` 或参考 mod 取；半透明件见 §6
- **法线贴图的 G 通道方向跟 V 轴绑定**：UE4/UE5 是 DirectX 风格（green-down），
  Bannerlord 也是 D3D ⇒ **不翻**。自查技巧：如果给 Blender 的 UV **翻了 V**（因为源是 top-origin），
  那 G 在 Blender 里就"自动正确"—— 两边是同一件事，不要重复翻。
- ★★ **资产 GUID 必须每个工程都不同**（见 §10.6）。自查：把两个 mod 的 GUID 求交集，
  必须是 0 —— 否则两个 mod 同时加载时引擎按 GUID 索引资产、互相覆盖，
  表现为**只装一个正常、两个一起装就贴图紊乱**。

---

## 6. ★★ 贴图与透明：最容易翻车的一节

### 6.1 带 alpha 的贴图必须声明 `systemFlags=["has_alpha"]`

**引擎判断"这张贴图有没有 alpha 通道"看的是资产标志，不是文件里有没有 alpha 像素。**

没声明时的表现极具迷惑性：材质侧 `blendMode` / `alphaTest` / alpha 数值**怎么改都不生效**，
渲染结果**跟着贴图颜色走** —— 深色贴图 = 黑片、亮色贴图 = 白片。

```bash
mbtool tex  <pack> <贴图名> | grep systemFlags     # 自查：应为 [has_alpha]
```

对照：原版 `battania_dress_c_d`、参考 mod 的 `body_D` 都是 `[has_alpha]`。
手写打包器极易漏这一条（本工程为此折腾了 4 轮）。**spec 里给所有 DXT5/带 alpha 的贴图加上它。**

### 6.1b alpha 未必是"镂空遮罩"——先量分布再决定

拿到带 alpha 的贴图，**先看 alpha 的直方图**，不同语义要用不同管线：

| alpha 分布 | 语义 | 做法 |
|---|---|---|
| 有大量 0、二值化明显 | 真·镂空遮罩（卡片/网格布） | alpha test（`battania_dress_c_alpha_mat`，alphaTest≈0.27） |
| **没有 0，中位 ~90、范围 20~170** | **明暗/覆盖度**（实测 RE4 头发就是这样） | 全不透明 → 发梢一根根竖着很毛躁；**alpha test 剪掉最外层虚发**轮廓才干净 |
| 中位 ~10、边缘 255 | 透明度（镜片） | `modulate` 管线，见 §6.2 |
| 全 255 | 没有 alpha | 不透明，别浪费 DXT5 |

头发那条实测最省事：`CHA200_20_hair` 的 A 通道中位 91、25% 低于 70、**一个 0 都没有**。

### 6.2 透明的两个条件（缺一不可）

1. **贴图带 alpha**（§6.1 的 `has_alpha` + 实际 alpha 像素）
2. **材质不能是 `no_alpha_blend`** —— 走透明管线：
   - `modulate`（modulator）：**相乘**语义，结果 = 背景 × 贴图RGB。所以镜片这类要给它**亮**贴图，
     否则背景被压黑
   - `alphaTest > 0` + `shaderMatFlags=[alpha_test]`：**镂空**（低于阈值整片丢弃）——
     源 alpha 若"镜身近 0、高光边缘 255"，配上阈值就得到"通透玻璃 + 边缘反光"
   - 原版真半透服饰 `khuzait_dress_b_alpha_mat` = `modulate` + `alphaTest=0.2745` + `alpha_test`
   - 相关 shaderFlag：`use_albedo_alpha`（显式启用 albedo 的 alpha）、`disable_vertex_color_alpha`
3. 换模板时**必须检查 `vertexLayoutFlags` 里有没有 `skinning`** —— 掉了它网格不跟骨骼动
   （打包日志里 `layout=[bumpmap]` 就是判据，应为 `[bumpmap,skinning]`）

### 6.3 自己生成的贴图：背景不能留大片纯黑

1024² 里只画两个小岛、其余全黑 → 小尺寸/远处时引擎采样**高 mip**，把内容与黑背景平均掉
（实测"眼睛纯黑"就是这个原因，而 Blender 近距渲染 mip0 看着正常，极易误判）。
**背景填成该内容自身的平均色**，并逐 mip 复核最暗值。

### 6.4 贴图通道语义（RE 系）

`_albm/_albmsc` 是基色（A 通道**可以**当透明度用，实测镜片区中位 10、高光边缘 255）、
`_nrmr` 是法线+粗糙度打包、`_atos` 是 alpha/SSS/遮蔽。
材质槽位编号在不同模板里语义不同（自己 dump 模板的槽位贴图名对照：`_d` 基色 / `_n` 法线 / `_s` 高光 / `_c` 色图）。

---

## 7. 装备 XML 与槽位

```xml
<Item id="vh_body" name="{=vh_body}名字" mesh="vh_body" Type="BodyArmor" weight="8" appearance="1.5">
  <ItemComponent><Armor body_armor="40" covers_body="true" covers_hands="true"
      has_gender_variations="false" modifier_group="cloth_unarmoured" material_type="Cloth"/></ItemComponent>
  <Flags UseTeamColor="false" Civilian="true"/>
</Item>
```

槽位经验：

- **头部槽（HeadArmor）一次只能穿一件** → 脸和头发必须放进**同一个 mesh**，否则戴上脸就没头发
- 原版角色的腿脚不会自动隐藏：鞋袜要做成 **LegArmor + `covers_legs="true"`**，
  否则原版的脚会从你的鞋子里露出来
- 一个 `<Item>` 里 `<Flags>` **只能有一个**（`maxOccurs=1`），多写会 schema 报错
- 物品栏搜索按**显示名**过滤，不是 id
- ★★ **XML 注释里不能出现 `--`**（XML 规范禁止）。用一排减号当分隔线
  （`<!-- ---------------- 月清疏 ---------------- -->`）会让 `ElementTree` 报
  `not well-formed (invalid token)`；Bannerlord 的 .NET XmlReader 同样会**拒绝整个文件**，
  表现是"**mod 加载了但一件装备都没有**"。分隔线用 `====`。
  验证里加一条 `ET.parse()` 把关。
- **`covers_*` 用差集定，别自创**：把原版（Native+SandBoxCore+SandBox）+ 几个参考 mod 的
  全部防具 dump 出来做属性差集，实测 **`covers_legs` 从不出现在 BodyArmor 上**（194+29 件里 0 例），
  只在 LegArmor 上。`hair_cover_type` 只有 `all` 一个取值。

---

## 8. 验证：五层，越早越省时间

| 层 | 手段 | 判据 |
|---|---|---|
| 资产正确性 | `mbtool metacheck` / `segcheck` | `checksum ok=N bad=0`、`metadata identical=N differ=0`、各段 `identical` |
| ★ **从最终产物倒推** | 解 `.tpac` 的 `VertexStreamData`，把 14 个数组按尺寸表读回来 | 权重和==255 占比 100%、索引 0..27；三角总数与规格逐个 metamesh 一致；每个材质组都能对上子网格 |
| 形状/姿势 | Blender 无头渲染（正面/侧面/顶视 + 按权重自动取景的特写）；逐骨区域拟合相似变换看残差 | 残差 ~0 = 该区域刚性未被揉过（实测脸 0.07mm、头发 0.00mm、手 0.00mm） |
| ★★ **动画形变**（离线摆姿势） | `pose_test.py`：纯 LBS 套一个合成姿势（walk / knee 屈膝 85° / stride），写 `work/<pose>/`，再用 `render.py --mode=<pose>` 渲染 | 膝/肘不塌角、裙摆不折、鞋底不陷地 |
| 权重落位 | `audit_bones.py`（每根骨的权重顶点离自己骨段多远） | 无骨的中位距离异常偏大（脚趾骨 z 范围只应有 [0.002,0.061]） |
| 跨 mod 冲突 | 两个包的 GUID 求交集 | 必须为 0 |
| 实机 | 进游戏看观感（UV/法线/透明/材质模板只能这样判） | 见 §9 |

### 8.1 ★★ 离线摆姿势工装（本工程性价比最高的一件工具）

**膝部错位、裙摆折痕这类问题只在"动起来"时才暴露**，而实机一轮很贵、还常常说不清姿势。
用一个合成姿势离线复现，就能本地迭代：

```python
v' = Σ w_b · (R_b·(v − h_b) + h_b)      # 纯 LBS，h_b = BL 关节位置
```

- 姿势表写 `(骨, 轴, 角度)`，**子骨按 `BL_NEXT` 继承父骨旋转**。
- ⚠️ **旋转传播只能走一趟**：链表已按"父下标 < 子下标"排好即拓扑序。
  写成"迭代 4 趟"会把旋转反复复合，实测把手臂甩到天上（bbox z 2.148）。
- ⚠️ **渲染脚本的 `--mode=X` 要真的读 `work/X`**：一开始把非 `posed/raw` 的 mode
  一律映射到 `work/weights`，于是 walk/knee/stride 三张图渲染出来**字节完全相同**，
  白查了一轮。

**★ 最有效的用法：做"源权重对照组"。** 用**源自己的权重 + 同一套旋转**再渲一遍，
就能区分「**源模型本身如此**」和「**我们的归并造成的**」——
膝部那个问题就是靠这个确认"我们确实比源差"、而不是模型自带缺陷。

**渲染脚本用自己简化材质**，所以**判定不了 `.tpac` 材质模板相关问题**（透明、shader 效果）——
那些必须实机。反过来，形状问题（扭曲、错位）渲染就能看出来，别浪费实机轮次。

---

## 9. 症状 → 根因速查

| 症状 | 根因 |
|---|---|
| 颜色错位 / 袜子变深蓝 / 鞋色不对 / 衣服凭空多褶皱 | UV 的 V 轴没翻（或翻错时机，切线手性错） |
| 半透明件是**纯黑片** | 材质 `no_alpha_blend`，或（更常见）**贴图缺 `has_alpha`** → 深色贴图被当不透明 |
| 半透明件是**纯白片** | 同上，但贴图是亮色（颜色跟着贴图走 = 被当不透明） |
| 远处/小物件变黑，近看正常 | 自生成贴图的黑背景被高 mip 平均掉 |
| 脸型扭曲 | 用了自算权重（面部几百根微骨）→ 改用原始权重 + 微骨显式归 head |
| 拇指/手指弯折 | 手指骨散到前臂扭曲骨 → 全部归 hand(19/26) |
| 手肘向后弯、双手端着 | 手臂整链刚性 → 改逐骨 + 链式落点（量化判据见 §4.2） |
| 肩臂太靠后 | 绕**锁骨根部**转肩+臂（不是整体前倾躯干） |
| 挺胸/肚子凸 | 整体前倾躯干的副作用；只动肩 |
| 头偏小、脖子看不见 | 头缩放/抬升（绕颈关节） |
| 体型太壮 | 按骨权重分区做 X 收窄；手臂保持 1.0 |
| 原版脚露出来 | 缺 LegArmor + `covers_legs` |
| 头发/睫毛不透明、边缘发白 | 同"纯黑片"：贴图缺 `has_alpha`（镂空失效） |
| 网格不跟骨骼动 | 材质模板 `vertexLayoutFlags` 缺 `skinning` |
| 打包报 `blob too small for mip 1` | `.bc` 少了 mip 链 |
| **小腿/某段肢体在动画里不自然弯曲、扭折** | 辅助骨按**名字**映射错了段（`L_Toe_Twist_s` 名字是 Toe、位置在小腿）→ 用 `audit_bones.py` 查"权重顶点离自己骨段多远"，加位置校验否决 |
| **鞋跟/脚陷进地面**（或整个人浮空） | 源骨架与 BL 的**脚踝离地高度**约定不同（高跟鞋尤其明显）→ 给下半身加竖直抬升场（§4.6） |
| **前脚掌/鞋头被压扁** | 脚骨与趾骨**各自**钉到 BL 关节位置，而源与 BL 的"踝→趾"方向差很多 → 趾骨落点改为由脚骨链式推出（§4.6） |
| **高跟鞋鞋跟变斜 / 鞋型被扭 / 手套变形** | 脚(手)骨被"最小旋转"掰到了 BL 约定，而鞋/手套是连姿态一起建模的 → 该骨**不做轴向对齐**，只用全局相似变换（§4.6） |
| **长裙/长袍和腿穿模** | 裙摆布料链整条归了骨盆 = 钢体裙 → 按位置把一部分权重让给同侧腿骨（§4.8） |
| 人被改矮了（修完脚底落地之后） | 抬升场等价于压短小腿 → 用绕地面的整体均匀放大补回（§4.9） |
| **只装一个 mod 正常，两个一起装就贴图紊乱** | 两个包的**资产 GUID 撞车**（打包器固定随机种子）→ 换种子策略（§10.6） |
| 头偏小（或放大后脖子/领口撕开） | `HEAD_SCALE` 要带渐隐，别整体放（§4.7） |
| 某个 mod 单独看正常，和别的 mod 一起就错 | 先查 GUID 交集，再看材质/贴图模板是否有跨包引用 |
| **整个模型像刚体一起摇晃、四肢不分开动** | `.mgeo` 里的权重没量化：0..1 的 float 被 `astype(uint8)` 截断成 0/1 ⇒ 绝大多数顶点权重全 0 ⇒ 引擎退化成一整块跟骨盆。**从最终 `.tpac` 读回 BoneWeights 自查**（§5） |
| **同一材质的"袖子在、上衣不在""靴子在、裙子不在"** | `.mgeo` **文件名同名覆盖**（一个 metamesh 里同材质有多个组）⇒ 只剩最后一个组。离线渲染读 npz 完全正常，所以极具迷惑性（§5） |
| **mod 加载了但一件装备都没有** | `items.xml` 注释里有 `--`（XML 非法）→ .NET XmlReader 拒绝整个文件。分隔线用 `====`（§7） |
| **颈部被拉长、衣领掉到肩线、"头被拔高了"** | BL 的锁骨/颈关节相对更低而头一样高 ⇒ 三者都钉 BL 位置就把余量摊在脖子上 → 颈上提 + 头下压（§4.6） |
| **裙子出现一道竖折痕（尤其迈步时）** | 裙摆按 `\|x\|` 分了左右 ⇒ 中缝是硬边界、被两腿反向剪切 → 改成**左右对称**跟腿（§4.8） |
| **膝/肘在动画里塌成多面体（"膝关节错位"）** | 归并到 27 骨后过渡带只剩一两圈边 → 沿网格边权重平滑 + 沿股骨的宽单调混合带（带宽要 > 源本身的过渡带）（§4.10） |
| **鞋底/手心莫名其妙陷进地面或飞出去** | 权重平滑/布料重算把小腿/前臂权重渗到了末端，而逐骨刚性映射会**外推**骨段外的顶点 → 末端排除（§4.10 / §4.8） |
| **改了参数但实机"看起来没变化"** | 先量"改动前后的平均像素差"确认改动是否真的落到产物上；再看判据里有没有**写死的绝对阈值**（实测同一条门在长裙上有效、在短裙上把整件削掉） |

---

## 9.5 编码/环境类坑（Windows）

| 坑 | 现象 | 做法 |
|---|---|---|
| GBK 控制台 | `python` 打印中文/`²` 直接 `UnicodeEncodeError` 中断管线 | 每个脚本开头 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`；命令前 `export PYTHONUTF8=1 PYTHONIOENCODING=utf-8` |
| `shutil.rmtree(ignore_cleanup_errors=True)` | Python 3.13 上直接 `TypeError`（那个参数属于 `tempfile.TemporaryDirectory`） | 用 `ignore_errors=True`，**并加一条"输出目录内容必须恰好等于期望集合"的自检**，否则残留文件会静默过关 |
| 幂等性 | 旧的 `.bc`/`.mgeo` 残留 → 打包进去了旧产物 | 每轮先清空输出目录 + 断言文件集合 |

---

## 10. 方法论（比任何参数都值钱）

1. **要判断某个效果怎么做，先 dump 一个"已知能正常工作"的同类产物做参数差集。**
   参数空间靠推理试错会空转很多轮（本工程在镜片透明上白跑了 4 轮，最后是
   `mbtool matall` 对比参考 mod 才定位）。
2. **"像素数据"和"资产元数据"是两回事。** 反复验证 `.bc` 里的 alpha 字节，却从没验证贴图资产的
   `systemFlags` —— 后者才是引擎的判断依据。
3. **缓存键必须包含影响输出的全部参数。** 只比 mtime 的缓存会在改了编码参数后静默复用旧产物
   （实测：改了 alpha 参数但 `.bc` 被判"新鲜"，管线全绿、产物是旧的）。
4. **改完先量化再动手**：把"我以为"换成数字（落点偏差、残差、面数、mip 最暗值）。
5. **实机反馈是唯一的最终判据**，但要让每一轮实机都"能区分假设" ——
   例如用纯色贴图做二分（网格是否在渲染），而不是再猜一次参数。
6. **★ 决定"产物身份"的值必须随工程变化，不能写死。**
   打包器里 `new Random(20260910)` 这种固定种子是个陷阱：所有用同一工具构建的 mod 会拿到
   **完全相同的 GUID 序列**（连消耗顺序都一样），于是两个 mod 同时加载就互相覆盖
   （实测两个工程 31 个 GUID 全部撞车，表现为"单独正常、一起装就贴图紊乱"）。
   正确做法：**先定包 GUID，再由它派生随机源** —— 同一个 spec 仍可复现，不同包必然不同。
   同源教训还有"缓存键必须包含影响输出的全部参数"（§10.3）。
7. **阈值和经验值都是有"适用模型"的，换模型必须重新量。**
   本工程多次因为照搬上一版的经验值翻车：缝合片阈值 6cm（删掉 4.68% 正常衣料）、
   UV 的 V 轴方向（上一版要翻、这一版翻了就鼻子跑到额头上）、
   裙摆的 z 门（为长裙设的阈值把短裙整件削掉）。
   **拿到新源模型，先把每个判据在本模型上的分布打出来，再定阈值。**
   ★ 尤其注意**写死了具体数值的门**（`z < 0.62`、`最长边 > 6cm`）—— 它们隐含了上一个模型的尺寸。
8. **★★ 验证必须落到"最终交付的那个文件"，而且必须覆盖"被调用路径"。**
   本工程连着栽了两次，而且第二次比第一次更隐蔽：
   - 第一次：渲染验收只读中间 npz，**从不经过 `.mgeo` / `.tpac`** ⇒
     离线全绿、进游戏少一半衣服（`.mgeo` 同名覆盖）。
   - 第二次：量化函数**写对了、也从没被调用**（`main()` 里漏了那一步）⇒
     整块模型变刚体。函数存在、单测想过，都不代表它在执行路径上。
   **做法：凡"由代码生成、再被别的工具消费"的中间文件，都要有一条从最终产物倒推回规格的断言。**
9. **改完先量化"改动有没有真的生效"。** 实机反馈"看起来跟上轮一样"时，
   先做 A/B（把新加的 pass 关掉重建、同一姿势各渲一遍）量平均像素差：
   实测 **1.05~1.31/255** —— 确认"改动真实存在但幅度小"，
   于是把精力放到"判据为什么不适用于这个模型"，而不是再猜一次参数。
10. **多角色工程要按"每个角色"检查判据。** 同一套参数在一个角色上明显见效、
    在另一个上几乎为零，往往不是 bug 而是**判据里的绝对阈值只适配了其中一个**
    （长裙/短裙、高个/矮个）。

---

## 11. 快速上手命令

```bash
export PYTHONUTF8=1 PYTHONIOENCODING=utf-8      # 中文输出不乱码
MB=/d/mb-tools/mbtool/bin/Release/net9.0/mbtool.exe      # 需先 dotnet build（见 mb-tools README）

$MB info   pack0.tpac                     # 资产统计
$MB matall pack0.tpac > all.tsv           # dump 全部材质参数（做差集用）
$MB mat    pack0.tpac <材质名>             # 单个材质：blendMode/alphaTest/flags/layer/槽位
$MB tex    pack0.tpac <贴图名>             # 单个贴图：尺寸/mip/格式/**systemFlags**
$MB mesh   pack0.tpac <网格名>             # 子网格 → 材质、包围盒、骨骼
$MB metacheck pack0.tpac                  # 校验和 + metadata 往返
$MB segcheck  pack0.tpac <资产名>          # 逐段字节比对（写入器是否保真）
$MB exportmesh pack0.tpac <网格名> <前缀>   # dump 几何到 <前缀>.bin + .json（可用来核对打包结果）
$MB roundtrip pack0.tpac out.tpac         # 读入再写出，验证读写器对称
```

### 自建工具（两个工程都带，可直接抄）

| 脚本 | 作用 |
|---|---|
| `tools/orient_report.py` | ★ 换模型必跑：**朝向**（脚趾 vs 脚踝、眼睛 vs 颅骨）+ **UV 的 V 轴实测**（按高度分带打 v 中位数）+ dump 骨架表 |
| `tools/audit_skin.py` | ★ **从最终 `.tpac` 读回蒙皮数据**：权重和==255 占比 / 非零占比 / 索引值域 |
| `tools/audit_bones.py` | 每根引擎骨"拿到权重的顶点离自己骨段多远"，抓权重跑错骨 |
| `pipeline/pose_test.py` | ★ 离线摆姿势（walk / knee / stride），看关节与布料形变 |
| `pipeline/verify.py` | 一键跑完所有检查（资产/打包网格/蒙皮/GUID/权重落位/XML/姿态与地面） |

### 从 `.tpac` 里读回顶点流（`audit_skin.py` 的核心，值得单独记）

`.tpac` → 资产 → 段；对 `VertexStreamData` 段：`i32 索引数` → `索引数组` →
`14×2 个 u64 尺寸表（偏移相对表自身开头，故首个数组偏移必为 224）` → 14 个数组。
用 `lz4.block.decompress(blob, uncompressed_size=段.actual)` 解压。

```python
n = struct.unpack_from('<i', d, 0)[0]
hdr = 4 + (n*2 if n < 65535 else 4 + n*4)
tbl = struct.unpack_from('<28Q', d, hdr)          # 校验：tbl[0] == 224
names = ['Colors1','Colors2','Uv1','Uv2','Positions','Positions2','Normals','Tangents',
         'BoneWeights','BoneIndices','CompNormals','CompPos','CompTangents','QTangent']
```

以上命令与工具覆盖了绝大部分排查；剩下的按 §10 的方法论自己取证。
